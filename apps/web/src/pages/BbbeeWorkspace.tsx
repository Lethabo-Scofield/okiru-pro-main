import { ProductWorkspace } from '@/components/product/ProductWorkspace';

/** `/bbbee` — the companies this consultant measures for B-BBEE. */
export default function BbbeeWorkspace() {
  return <ProductWorkspace product="bbbee" />;
}
