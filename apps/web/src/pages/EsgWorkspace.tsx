import { ProductWorkspace } from '@/components/product/ProductWorkspace';

/** `/esg` — the companies this consultant reports on for ESG. */
export default function EsgWorkspace() {
  return <ProductWorkspace product="esg" />;
}
