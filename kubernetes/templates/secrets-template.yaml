# SECRETS TEMPLATE
# IMPORTANT: This file contains placeholder secrets for structure only.
# DO NOT use these values in production.
# These secrets should be generated from .env.production using the scripts/create-secrets.sh script
# or created manually via kubectl with actual secure values.

---
apiVersion: v1
kind: Secret
metadata:
  name: mongodb-credentials
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: "PLACEHOLDER_CHANGE_ME"
  MONGO_INITDB_ROOT_PASSWORD: "PLACEHOLDER_CHANGE_ME"
  MONGODB_URI: "PLACEHOLDER_CHANGE_ME"
  MONGODB_DB_NAME: "PLACEHOLDER_CHANGE_ME"

---
apiVersion: v1
kind: Secret
metadata:
  name: arangodb-credentials
type: Opaque
stringData:
  ARANGO_ROOT_PASSWORD: "PLACEHOLDER_CHANGE_ME"
  ARANGO_URL: "PLACEHOLDER_CHANGE_ME"
  ARANGO_DB_NAME: "PLACEHOLDER_CHANGE_ME"

---
apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
type: Opaque
stringData:
  REDIS_PASSWORD: "PLACEHOLDER_CHANGE_ME"
  REDIS_URL: "PLACEHOLDER_CHANGE_ME"

---
apiVersion: v1
kind: Secret
metadata:
  name: session-secrets
type: Opaque
stringData:
  JWT_SECRET: "PLACEHOLDER_CHANGE_ME"
  SESSION_SECRET: "PLACEHOLDER_CHANGE_ME"
  API_INTERNAL_KEY: "PLACEHOLDER_CHANGE_ME"

---
apiVersion: v1
kind: Secret
metadata:
  name: acr-pull-secret
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: "PLACEHOLDER_BASE64_DOCKERCONFIG"

---
# Template for external API keys and third-party integrations
apiVersion: v1
kind: Secret
metadata:
  name: external-api-keys
type: Opaque
stringData:
  # Add external API keys as needed
  EXTERNAL_API_KEY: "PLACEHOLDER_CHANGE_ME"

---
# Azure Blob Storage credentials for backup sync
# Used by backup CronJobs to sync to Azure for off-cluster durability
apiVersion: v1
kind: Secret
metadata:
  name: azure-backup-credentials
type: Opaque
stringData:
  AZURE_STORAGE_ACCOUNT: "PLACEHOLDER_CHANGE_ME"
  AZURE_STORAGE_KEY: "PLACEHOLDER_CHANGE_ME"
