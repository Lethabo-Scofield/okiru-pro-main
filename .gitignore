node_modules/
dist/
.env
.env.*
!.env.example
!.env.production.template
*.log
.DS_Store
*.tsbuildinfo
*.tgz
*.tar.gz
.turbo/
.cache/
coverage/
.nyc_output/
__pycache__/
*.pyc
*.pyo
.venv/
*.pptx
*.pdf
bee-execution-plan-extracted.txt
assets/
scripts/set-secrets.js

# Domain extraction artifacts (large/generated)
docs/domain/
docs/slides/
docs/speaker_notes.json
docs/slide_extraction_raw.json
docs/export_slides_images.ps1
docs/extract_pptx.py
docs/TRAINING_PACK_EXTRACTED.md
acr_logs.txt

# Kubernetes secrets & credentials (never commit these)
*-secrets*.yaml
*-credentials*.yaml
*-api-keys*.yaml
temp-secret-patch.json

# graphify tool output (generated cache/graph artifacts)
graphify-out/
apps/graphify-out/

# Local credentials / cluster access — must never be committed (untracked 2026-07-26;
# note: still present in git HISTORY, so the exposed creds must be rotated).
kubeconfig*.yaml
az_accounts.json
app-secrets*.yaml
*-credentials-*.yaml
*.pem
*.key

# Build/tool logs and scratch artifacts
acr-*.txt
acr-*.log
