"""Namespace package root for okiru-* shared Python components.

This module is intentionally empty so multiple `okiru-<name>` distributions
can coexist under the same import root via PEP 420 namespace packages.
"""
