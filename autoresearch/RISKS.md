
## M7b — Import validation noise (W-validate + W3) — 2026-06-25
Live import flagged 700–2,900 non-blocking "issues" per workbook (looked broken). Root
causes + score-NEUTRAL fixes (commit b5d7e84a; Lake 63.56 + SCORE 12/14 unchanged; web
suite green; 28,943 → 181 total issues, 0/16 blocked):
- **W-validate (Yes/No flood, ~95%):** `yesNoColumn` fields are `yesNoBoolean` → importer stores JS booleans → strict-select stringified to "true"/"false" → rejected vs ["Yes","No"]. Fix: tolerant `selectValueAllowed` in workbookValidation.ts (booleans/1-0/y-n + case-insensitive + ESD/SED contribution synonyms). VALIDATION-ONLY — an earlier attempt to canonicalise at ingestion shifted an ESD score (Kgodiso 112→111), so all leniency now lives in the validator.
- **Junk shareholder rows:** "Black voting rights | 63%" summary rows showed as bogus shareholders (% in Race col) in the preview. Filtered at ingestion (projection already dropped them for scoring).
- **W3 name/surname split DONE:** full name in one "Name" column + separately-required "Surname" → 500 "surname Required" warnings/file. Split "First Last" at ingestion. Resolves the user-reported split bug.
- **Remaining 181 = genuine:** real cross-field check (black-female > black ownership, ×65) + once-per-workbook missing-optional-field notices (skills meta: headcount/training-manager-salary/period/date/EAP — relaxing these `required` flags is a product call, deferred). Deployed to prod.

## M7 — Workbook-weakness hunt: all 16 Toolkit Testing Data → Level 1 (2026-06-24)

**Goal (expert ground truth):** every workbook in `docs/Toolkit Testing Data` must score
**Level 1** on import. **Fitness:** `apps/web/src/__tests__/toolkitTestData.score.harness.test.ts`
(`TOOLKIT_SCORE=1 npx vitest run ...`). **Baseline: 0/14 scored are Level 1** (~45–68% vs the
~95–100% Level 1 needs; Transport ×2 = no bundled config, API sector).

Open weaknesses to investigate (find the ROOT CAUSE — under-INGESTION, not score inflation):
| # | Weakness | Evidence | Status |
|---|---|---|---|
| W1 | Systematic under-scoring on import (every sector ~half marks) | score harness baseline 0/14 | OPEN — likely financials (TMPS/NPAT) not feeding calculators + pillar data dropped |
| W2 | FSC sub-variants (Banks/LTI/STI) collapse to FSC Generic | harness shows all FSC = "Generic" config; meta.fscSubSector not derived | OPEN (also R20) |
| W3 | Name/surname not split on import | user report; employees ingest single `name` not first/last | OPEN |
| W4 | Lake Trading "shortcut" scores 62 live vs 63.53 headless | autoresearchFitness comment "~62.17 RED — MC per-demographic EAP under-scores" | OPEN — live vs fitness MC discrepancy |
| W5 | Upload (vs import) flow crashed twice (companyName, length) | fixed defensively; root = incomplete fallback previewResult object | DONE (bf9a0ea4) — watch for more |
| W6 | Validation too far-fetched / blocks legit data | user wants validation-robustness test | OPEN |
| W7 | Construction scores partial (NC) — 13 inputs missing | expected; Phase-1 tail | KNOWN |
| W8 | Transport not scorable headlessly (config from API) | harness "no bundled config" | OPEN — bundle a Transport config or score via API |

**Method:** Chrome-in-Claude IS connected (Browser 1) — use it to verify the LIVE path for
discrepancies (esp. W4). For each weakness: reproduce in the score harness, find why the data
is dropped/under-scored, fix the IMPORT/normalizer (not the targets), re-run the fitness, keep
the full suite green. **Goal: 16/16 Level 1.** Do not inflate scores — fix ingestion.

### M7 iteration 1 (2026-06-24) — ownership + ESD ingestion
- **W1a DONE** (ESD sheet "Enterprise & Supplier Developme" skipped → no SD/ED): added truncated-name hints. SD now ingests.
- **W-own DONE** (3 bugs: junk summary rows ingested as shareholders; flat 1-share weighting; blackOwnership=max(voting,EI) squared by the calc): filter junk, derive shares from voting%, set blackOwnership=1.0 for black individuals. Ownership 12→23-25/25.
- **Impact:** scores lifted from ~45–69 to ~66–90; best now AGRI ~90/132 (L3). Still **0/14 Level 1**. No regression (Lake 63.53; suites green).
- **Next (biggest remaining gaps):** W1b `ed0` everywhere (enterprise-dev contributions not categorized, ~7pts); FSC `sk1` (FSC skills broken, ~20pts on FSC); the remaining ~10–30pts to L1 per workbook.

### M7 iteration 2 (2026-06-24) — ESD category + skills column collision = 0->7 Level 1
- **W1b DONE** (ed0: ESD sheet has no category column; SD/ED encoded in Contribution Description): derive category from description. ED now scores.
- **W-skills DONE** (THE big one: "Salary Cost (category…)" + "Location" headers substring-matched "category" and overwrote the real Category column via last-write-wins → categoryCode got the Province value → category-based skills scored ~0 for ALL sectors): two-pass exact-match-first column mapping in parseGridFromSheet. Skills RCOGP 9→22, FSC 1→19.
- **Impact: 0/14 → 7/14 Level 1.** Lake 63.53 unchanged; suites green.
- **Remaining 7 not-L1:** mostly FSC Banks/STI variants (W2 — they use FSC Generic config + are criticalBlocked R20) + ICT Generic 116/140 (L2, close). Next: W2 fscSubSector derivation, then the per-workbook tail.

### M7 iteration 3 (2026-06-24) — supplier enterpriseType + procurement = 7->8 Level 1
- **W-proc DONE** (QSE/EME spend = 0: the W-skills two-pass nulled "Current Company Size *" because its first substring match (sizeAtFirstProcurement) was claimed; currentSize never set -> all suppliers enterpriseType=generic): mapHeaderToKey now takes excludeKeys; substring pass skips claimed keys and falls through to the next valid one. Procurement pp 18->27-29.
- **Impact: 7/14 -> 8/14 Level 1** (ICT Generic reached L1). Lake 63.53; 112 golden/normalizer tests pass.
- **Remaining 6 not-L1: ALL FSC Banks/LTI/STI variants** (~102-104/130-132, L2). They have FSC-specific elements — AFS (12pts), Consumer Education, Empowerment Financing — that the fitness (and possibly the live calculateScorecard) does not score/ingest. NEXT: determine whether the live FSC path scores those elements; if yes, add them to the fitness + ingest their sheets; if the data/scoring genuinely isn't there, flag the FSC variants for the expert.

### M7 iteration 4 (2026-06-24) — FSC AFS ingestion = 8->12 Level 1 (FSC cluster SOLVED)
- **W-fsc DONE** (commit 4569c38d). ROOT CAUSE: FSC workbooks express Access to Financial Services as a **generic indicator table** (`Access Indicator | Unit | Notional Target | Achieved | Qualifying?`), NOT the structured per-field afs-additions meta the mapper expects (`afsActiveAccountsCompliant`, `afsElectronicAccessCompliant`, …). `parseMetaFromSheet` matched nothing → `afs-additions` meta `{}` → `calculateAfsScore` got empty data → **AFS scored 0**, dropping all FSC Banks/LTI/STI to L2 purely on unscored AFS. Empowerment Financing maxPoints=0 (not scored) and Consumer Education is not a separate scored element, so AFS was the entire gap.
- **FIX (faithful under-ingestion, no target change):** new `deriveAfsMetaFromIndicatorTable` in the normalizer reads the table's "Qualifying" column (explicit Yes, or Achieved≥Target) and sets the matching afs* compliance flag, so the EXISTING `calculateAfsScore` scores it (both fitness AND live, via `mapWorkbookFinancialsToClient`). Indicators the sheet doesn't carry (geographic point-coverage %) left unset → 0, never fabricated. Added `calculateAfsScore` to the score harness for FSC.
- **Impact: 8/14 → 12/14 Level 1.** All four FSC Banks/STI workbooks now L1: Sechaba_Mutual Banks 104→112, Sechaba_Financial Banks 103→111 (afs 8/12 = activeAccounts+electronic+pointOfPresence); Vela STI ×2 102/103→113/115 (afs 12/12). Lake unchanged 63.56; web suite green (pre-existing `numericDateInput` date-test failure unrelated). Held for review — not deployed.

### M7 — FINAL STATUS (2026-06-24): 12/14 scored Level 1; remaining 4 flagged with evidence
The M7 mandate (the FSC Banks/LTI/STI cluster) is **solved**. Six clean, faithful, verified under-ingestion fixes lifted the fitness from **0/14 → 12/14 Level 1** (W1a, W-own, W1b, W-skills, W-proc, W-fsc) with Lake stable at 63.56 and suites green throughout. The remaining 4 are pre-tracked, partially expert-gated, lower-priority items — STOPPING per the guardrail (every gap flagged with specific evidence):

- **Construction ×2 — FLAG (W7, held Phase-1 work + expert config sign-off).** Khethiwe Contractor **71.5/123 (L6)**: Skills only **5.2/26** (missing 9 indicators), ESD 24.3/38, Ownership 20/31, MC 18/22 — **16 construction-specific fields not ingested** by `buildConstructionScoringInput`: newEntrantsPercent, blackProfessionalsPercent, blackYouthPercent, skillsAfricanEapPercent, skillsBlackMgmtExecSeniorMiddlePercent, skillsBlackMgmtJuniorPercent, skillsCatABCDPercent, skillsIndustryCandidatesPercent, skillsDisabilitiesProgrammesPercent, mentorshipProgrammeImplemented, absorptionPercent, mentorshipPromotionPercent, professionalRegistrationPercent, pp35BlackWomenOwnedSpend, ppBlackWomen51Spend, supplierContractorDevProgrammeImplemented. QSE Khethiwe **82/110 (L4)**: 7 missing (votingAndEconomicBlackPercent, votingAndEconomicBlackWomenPercent, skillsSpendBlackSecondary, skillsSpendBlackDisabledPercent, absorptionPercent, industryRegistrationPercent, pp35BlackWomenOwnedSpend). These are exactly the held "Construction Phase 1 remaining (~10 new inputs)"; needs the construction mapper extended + expert sign-off on the (UNVERIFIED) construction configs before forcing scores. NOT a simple ingestion bug.
- **Transport ×2 — FLAG (W8, fitness-coverage gap, NOT a production bug).** The headless fitness can't score them because the Transport config is fetched from the API at runtime (`/api/scorecard/sector-config/...`) and isn't bundled in `apps/web/Toolkit/src/lib/sectors`. The LIVE import path DOES score Transport (API `TRANSPORT_LARGE`/`TRANSPORT_QSE` configs in `apps/api/pipeline/sectorConfig.ts` + dedicated `Toolkit/src/lib/calculators/transport.ts`). To verify L1 headlessly, bundle a Transport CalculatorConfig into the fitness (handle the QSE elective-element selection) or call the API scorer from the harness — deferred (lower priority; production unaffected).

### M8 — 2026-07-03: 26th CEE EAP vintage system + Transport SOLVED (14/16 L1); Construction expert-blocked; full-chain audit queued
- **EAP (26th CEE, pp.33-34 + NPAT norms xlsx) ingested** (commit b3c490ae): year-keyed as 2026 = new default; `client.eapYear` plumbs the vintage (Settings picker); Lake demo/fitness/Lake-decimal goldens pin 2025 (their workbooks are 25th-CEE vintage) → Lake stays 63.53/GAP 0.03. Threshold-saturation + EAP-distributed tests left unpinned (vintage-agnostic invariants). Flagged for Zoleka, NOT changed: constructionScoring `DEFAULT_AFRICAN_EAP_PERCENT = 80.4` (26th CEE African total = 81.3%).
- **W8 Transport SOLVED** (commit 31450a7c): bundled `sectors/transport-generic.ts`/`transport-qse.ts` (converted from pipeline TRANSPORT_GENERIC/TRANSPORT_QSE = Transport Codes.xlsx); mirrored `calcTransportLarge*` MC 11/EE 18/Skills 15 into `calculators/transport.ts`; store scores Large MC+EE+Skills (EE 18 was counted-in-target-never-scored on the live path); harness scores both: **QSE 105/107 L1, Large 102/108 L1 → 14/16 Level 1** (all 16 scored).
- **W7 Construction ×2 — still expert-blocked.** `docs/Construction-Phase1-Inputs-Validation-Request.md` (2026-06-25) to Zoleka remains UNANSWERED (all ☐ unticked; open Q: QSE Skills "25% of leviable" 7-pt line — near-zero for everyone; "African People per Stats SA EAP" basis undefined). The 16 Generic + 7 QSE missing inputs need her sign-off AND workbook columns that the test workbooks predate. Do not force.
- **Full workbook→toolkit connection audit** (user mandate: "financial info in workbook wasn't in toolkit; breakdown points aren't reflecting"): 8-chain adversarial audit launched; transport chain completed (→ fix above); 7 chains (financials, ownership, mc-skills, pp-esd-sed, construction, live-vs-harness, sector-elements) hit the subagent session cap — resume scheduled 02:37 via one-shot cron (resumeFromRunId wf_6ae1f02b-b5f), then implement confirmed findings under the gates.

### M8 continued — 2026-07-04: workbook→toolkit connection audit landed (6 fixes; still 14/16 L1)
The full-chain audit (user mandate: "financial info in workbook wasn't in toolkit; breakdown points aren't reflecting") produced six verified, harness-neutral fixes. Every one validated against the real .xlsx before applying; Lake stayed 63.53/GAP 0.03 and the 16-workbook harness stayed 14/16 (the 2 gaps are the expert-blocked construction workbooks). NOT deployed — held for user review.
- **Financials read-path drop** (commit 1ebafc04, prior): GET /:id/data returned `industryNorm: undefined` and dropped the whole `client.financials` blob → live lost deemed-NPAT, combineExcoSenior, CE/Fundisa, AFS. Now returned. This was the KEY link — it makes the store's `finExtras` fallbacks (ceSpend/afs/eapYear/combineExcoSenior) actually resolve.
- **leviableAmount = full payroll, not payroll×1%** (d2297f7d): `mapWorkbookFinancialsToClient` computed the 1% SDL levy as the skills base → every live Skills target 100× too small. Cited Kgodiso: Total Payroll = Leviable Amount for Skills = 96M; 6% of leviable = R5.76M. Harness already used full payroll (why headless was right, live wrong). Fixed code + 3 tests that pinned the wrong ×1% target.
- **Board voting rights hydration** (f66c2889): import persists employee `votingRights`; store read only `votingRightsPercent` → board exercisable-voting weights dropped → silent headcount fallback. Store now accepts either (both %). Live-only (harness feeds projection direct).
- **constructionSubSector persist + leviable back-sync** (9934d60e): submit path never wrote `constructionSubSector` → live construction hydrated as Contractor default. Now copied from companyMeta. Also fixed `clientPatchToWorkbookMeta` reverse codec (`payroll = lev×100`) → identity, consistent with the leviable fix (was inflating payroll 100× on Toolkit→workbook back-sync).
- **FSC Consumer Education ingestion** (e0a78fae): the populated "Consumer Education" sheet (per-programme table) was unmapped → CE line scored 0 on every FSC workbook despite real data. New `deriveConsumerEducationSpend` sums the Spend column into `sed.meta.ceSpend` (mirrors the AFS indicator-table derivation). Aggregate = full Spend column, cited from each workbook's Sector Targets sheet (Sechaba 0.6% = R2.52M/R420M). FSC SED 3→5 (+2 base CE pts). Made the harness faithful (now passes ceSpend/ceBonusSpend/fundisaSpend, as the live store does). Unit test locks R2,520,000 + no-fabrication for non-FSC.
- **Empowerment Financing — FLAGGED (no calculator exists).** `docs/FSC-Empowerment-Financing-Expert-Request.md`: FSC Banks/LTI EF sheet carries R9.8bn advanced but scores 0 — no `calculateEmpowermentFinancingScore`, store doesn't compute it, config pins all EF sub-lines to 0 "pending expert sign-off". This is a scoring-RULE gap (targets/points don't exist), not ingestion — so left at 0 per the guardrail. Needs expert to define EF point structure + target basis. All 6 FSC workbooks already L1 without it.
- **Minor:** the score harness DIR = `resolve(process.cwd(), '../../docs/...')` only resolves when cwd=apps/web (fails from repo root). Cosmetic test-runner fragility; not touched.

### M9 — 2026-07-06: FSC Empowerment Financing scored + template-exact SD/ED/CE; ESG extraction fidelity
User asked to (1) implement FSC EF, (3) ingest CE bonus/Fundisa, (4) reconcile SD/ED, and bring ESG extraction to B-BBEE parity. All scoring numbers template-cited (docs/toolkits/extracted_formulas/FSC_Generic + docs/esg/Okiru_ESG_Toolkit_v1_7 xlsx). Lake 63.53, harness 14/16 held. NOT deployed — held for review.
- **FSC EF (Q44 resolved)** (commit 54772e27): the "0 pts pending sign-off" was a formula artifact (blank template defaults sub-sector→Others, EF cells =IF(D7="Banks"/LTI,pts,0)). New calculateEmpowermentFinancingScore (mirrors afs.ts): Targeted Investments 12 + Transaction Financing 3 = 15 pts, Banks/LTI only (null else). Banks TI = D7/SUM(C5:C6); LTI = C6/C5; TF = SUM(tblTransactionFinancingData[Value])/portfolio. Gathering-workbook facility tables score qualifying-weighted portfolio fraction (unstated Qualifying% = 0, no fabrication). Ingested the "Empowerment Financing" facility sheet → esd.meta.efFacilities → financials.empowermentFinancing → store/harness. Sechaba Banks 113/114 → 123/124 of 140.
- **SD/ED reconciliation** (33): template EF & ESD Scorecard rows are Banks/LTI-specific — SD 10→7 (C17/C16), ED base 5→3 (C19/C18); the old 10/5 were the Others ESD-scorecard maxima. Caps: ED 5 Banks / 7 LTI (incl. bonuses).
- **CE bonus/Fundisa** (32): Additional CE bonus DERIVED per template I10 = MAX(CE − 0.6%NPAT, 0) when not explicit; Fundisa + CE ingested from template-shaped "SED & CE Data" sheets via the Pillar column (tblSEDnCEData SUMIFS strings). Gathering workbooks carry no Fundisa → 0.
- **Totals/thresholds**: Banks 130→140, LTI 132→142; FSC_LEVELS_BANKS_LTI corrected W/111×120 → W/111×121 (template core F12=121 INCLUDES EF; L1 109.01). Goldens updated with cited values (14 assertions).
- **ESG extraction fidelity** (commits a1880329, 9f740571): NEW deriveEsgSummaryCells — the manual grids wrote raw cells (s1a_C14, hc_r_c, B13) but scorers read the template's summary cells (E_Data L19/L46/L63, S_Data L12, G_Data F5/F13..) that nothing recomputed → manual workbooks scored as empty (only fixtures that hand-injected summaries scored). Derives every scorer-read cell from raw inputs using verbatim template formulas (Okiru_ESG_Toolkit_v1_7 xlsx: L19=Σs1a, L46=Σs2, L63=Σwater, L12=Σhc, F5=IF(B5>0,5,0), F13..=Yes/Partial/No→5/2.5/0); never overwrites explicit values. Removed a real free-points bug (CSI _initiatives_count defaulted to full threshold → everyone got 5 pts; now derived from s-data-csi register, default 0). Unblocked submit (text cells read via new readEsgText, not number-coercing readEsgCell; validation derives summaries first). 53 ESG tests + 8 new fidelity tests green.
- **ESG remaining (flagged, NOT done):** the xlsx-IMPORT path is still dead (server has no express.raw for the octet-stream upload → 400) and its per-sheet routing needs work (S_Data OFO/CSI rows misroute to s-data; register grids misalign to the real v1.7 8-column layout; export drops company-setup/OFO/CSI sections). This is a bounded follow-up (like B-BBEE's normalizeExcelBuffer column mapping). The MANUAL entry path — how users actually enter ESG — is now B-BBEE-faithful.

### M10 — 2026-07-10: E2E source-document upload flow live locally (parser wired into UI)
- **Parser wired end-to-end** (commit 3fc55c14): new "Upload Source Documents" mode card (the old "Upload & Extract" had silently shadowed the multi-doc flow with the integrated-workbook path); web proxy streams multipart /api/parser/* → okiru-ai-parser :3200; upload page shows the parser's Document Coverage checklist (per-file classification + Lethabo's missing-required-documents + review flags); NEW Score Preview step between Extract and Review shows the approximate B-BBEE level/pillars via the same calculate-from-extraction mapping Submit uses (read-only) with the "fixing values can only move this up" nudge.
- **Verified in-browser E2E**: 3 sample docs (BEE certificate + sworn affidavit + supplier spend CSV) → all classified correctly → extraction (LLM hybrid) → Approximate Scorecard 38.0/120 (own 20/25 from cert's 51% ownership, PP 18/29 from 3 suppliers, 0 where no docs — honest) → Review Results with editable entities. Parser case output exact (supplier rows 1.2M/L2/51, 800k/L1/100, 450k/L4/30).
- **Real bugs fixed along the way**: apps/api crashed at startup — `google-auth-library` imported (Lethabo GA service) but never declared → added dependency (would have broken the NEXT api deploy); apps/api requireAuth now honors the proxy's trusted x-okiru-demo-user header outside production (Mongo-less dev; same pattern as adminAnalytics); offline demo user gets super_admin (dev-only path) so /processor is reachable.
- **TOOLING NOTE:** root pnpm install bumped vitest 2→4. The standing fitness command's `--poolOptions.forks.singleFork=true` flag is DEAD (CACError) — use `npx vitest run <file> --pool=forks` only. Fitness verified under AUTORESEARCH=1 hard gate: 2/2 passed (SCORE≈63.53 within tolerance).
- **Open (task #39)**: score parity vs the straight workbook upload for the same underlying data — mechanism is shared by construction (same calculate-from-extraction endpoint), exact-parity test not yet built. Parser→scoring handoff still rides the LLM extraction; feeding parser supplier_rows/calculator_payload directly into the score is the next deepening step. NOT deployed — local only.

### M10 continued — 2026-07-13: create-scorecard document-upload start (parser → canonical workbook)
- (commit d885864f) /create-scorecard now starts from documents: expected-docs checklist → parser resolve-case-files → **mapParserCaseToWorkbookSections** (supplier_rows → procurement grid rows incl. per-supplier cert expiry; Ownership Confirmation → aggregate 100%-equity row, trust-style encoding; SED confirmation → SED row; skills/MC aggregates surfaced in coverage, never fabricated) → shared createFromSections (also now used by Excel import — redundancy removed) → /api/workbook/:id/import + submit → **the canonical calculator chain**. Parity with straight upload by construction (#39 closed). E2E verified: 4 docs → workbook C-65543 pre-filled server-side with exact extracted values, scored by canonical submit. Guards: fitness hard-gate + 189 tests green.
- Prior batch (commit cef1ca78): processor flow reduced to two start options, stepper diagram removed, Arango templates retired (classify step deleted; sector/type from company info), EMS-style expected-documents panel from GET /api/parser/document-types (7 canonical + ~110-doc verification matrix). NOT deployed — local.

### M11 — 2026-07-11: sector-aware document catalog + parser cluster wiring (deploy prep)
- **graphify** rebuilt on okiru-ai-parser (335 nodes / 12 communities). Community 7 = the entire B-BBEE domain layer and contains ONLY Codes 100–500 (Generic) — graph-level proof the parser is Generic-only. Verification matrix v3 = 6 sheets (5 Generic pillars + TMPS), no sector sheets. graphify-out/ committed.
- **Owner question "is our required list of documents per sector accurate?" → NO, there was no per-sector list.** REQUIRED_DOCUMENT_GROUPS was a hardcoded 2-group Generic constant served to all 6 scored sectors (Generic/ICT/AGRI/Transport/Construction/FSC). Confirmed by the audit workflow (1 finding survived majority-of-3 adversarial verify before the session hit its limit) + direct file reads.
- **Fix (commit 4f9e61c3):** parser/sector_documents.ts — getRequiredDocumentGroups({sector,size,subSector}) grounded in the sector codes (Construction +CIDB/BEP professional reg; FSC +AFS/Consumer Ed/Empowerment Financing; Transport +operating licence; EME → single affidavit). autoExtract flags parser-readable vs evidence-only (honest, no fabrication). GET /document-types?sector=&size=&subSector=. DocumentUploadStart gets sector/subSector/size selectors that drive the checklist AND stamp industrySector/scorecardType/sub-sector into company-information meta → the created scorecard scores under the CORRECT sector calculator (was always defaulting to Generic — a latent scoring bug for non-generic uploads).
- **Second deploy blocker fixed:** okiru-ai-parser had a Dockerfile but NO k8s manifests, and web's apiProxy defaulted /api/parser/* → 127.0.0.1:3200 → would 502 in prod. Added deployments/parser.yaml (2 replicas, /health probes, non-root RO rootfs, optional Neo4j → in-memory fallback) + parser Service + PARSER_SERVICE_URL=http://parser:3200 on web + base kustomization + prod overlay image.
- Verified: parser tsc clean, 69+8 tests; web tsc baseline 80; Lake fitness gate held; live endpoint returns correct per-sector groups; UI selector reactive (FSC→+AFS/CE/EF); prod kustomize renders parser + PARSER_SERVICE_URL. Deploy: ACR cloud builds (parser+web, tag 4f9e61c3-20260711-135822) in progress, then surgical apply to okiru-pro-aks (sub cfc3d77c).

### M11 deploy — 2026-07-11: document-upload flow LIVE in production
- Built parser + web images (ACR cloud build; --no-logs to dodge the known colorama/cp1252 crash — verified tags via show-tags per guardrail, NOT exit code). Parser rebuilt once after finding a prod bug.
- **Pre-existing bug found via deploy:** getParserRepository() hard-threw in production when Neo4j wasn't configured (only fell back to the in-memory ontology in non-prod) → every /document-types, /resolve, /resolve-case 500'd in-cluster. Fixed (commit 4c8399ee): in-memory fallback now works in prod, gated by PARSER_REQUIRE_NEO4J=true. The in-memory ontology (7 canonical types + verification matrix) classifies/extracts fully — Neo4j is optional.
- **Ingress gap found via deploy:** /api/parser fell through to the api catch-all (no parser proxy) → public 404. Added /api/parser → web rule in base ingress + prod okiru.pro host patch (commit 10a128ee).
- **Deploy gotcha logged:** `kubectl apply -f <rendered-overlay> -l part-of=okiru-pro` re-applies EVERY labeled object, and reverted the parser image to the overlay's stale tag. Lesson: to surgically apply ONE object from a rendered overlay, extract that object — don't apply the whole file. Restored via set image + corrected the overlay tag.
- Deploy sequence (okiru-pro-aks, sub cfc3d77c, pinned): parser Deployment(2)+Service first → healthy → web set image 4f9e61c3 + PARSER_SERVICE_URL env → ingress. VERIFIED end-to-end publicly: GET https://okiru.pro/api/parser/document-types?sector=FSC → 200 with FSC groups; Construction Contractor → +CIDB. All 7 deployments healthy, okiru.pro 200.
- Live prod image tags: parser 4c8399ee-20260711-140538, web 4f9e61c3-20260711-135822, api unchanged 60a0e704.

### M12 — 2026-07-13: proceed-with-partial + per-document missing-content flags (LIVE)
- Owner ask: users must be able to proceed even without all required docs (score on what we have), and missing content INSIDE a document must be flagged during the flow.
- (commit 3c303cf2, web-only) DocumentUploadStart: create gate relaxed to company-name-only (was mappedRowCount>0) so missing docs never block; CTA adapts ("Build from N values" vs "Continue to workbook & complete it there"). Each file card flags what the parser couldn't read — named fields (validation.missing_fields → "Couldn't read Certificate Expiry, …") + notes (errors/warnings/review reasons, e.g. low confidence); passed+complete docs show "· all read". ParserCaseLike extended with validation + documents_needing_review. Scoring-on-partial already worked via the canonical workbook path — no parser/scoring change.
- Verified E2E local: lone low-confidence cert, no spend schedule → gap flagged, create enabled, landed in workbook C-63294. Guards: tsc baseline 80, fitness + mapper tests 9/9.
- Deployed web tag 3c303cf2-20260713-082508 to okiru-pro-aks (sub cfc3d77c, pinned). set image only; PARSER_SERVICE_URL preserved. Post-deploy: okiru.pro 200, parser proxy 200, all 7 deploys healthy.
