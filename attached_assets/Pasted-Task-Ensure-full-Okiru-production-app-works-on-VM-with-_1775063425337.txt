Task: Ensure full Okiru production app works on VM with all modules, including login, forgot-password, and email notifications.
Objectives:

Nginx Routing Fix:
/api/* requests go to web container (port 5001) for frontend-handled routes.
Backend-only routes (scorecard, extraction, admin) proxied to API container (port 5000).
Proper CORS headers and OPTIONS handling for both HTTP and HTTPS.
Frontend Safe Rendering:
Fix Cannot read properties of undefined (reading 'length') by adding Array.isArray(), ?., and || [] guards wherever template entities or company info are accessed.
Ensure template page, dashboard, entity builder, and document processor pages render safely.
Health Check Endpoint:
GET /api/health implemented on both web and API servers.
Authentication Flow:
Login form works correctly.
Forgot-password flow:
/api/auth/forgot-password (POST) accepts user email.
Send secure, time-limited reset token to user personal email (for now, use personal email addresses).
/api/auth/reset-password (POST) accepts token + new password, validates token, updates password, invalidates token after use.
Frontend: Login page has “Forgot Password?” link; reset page allows entering new password with token.
Optionally handle username recovery emails as well.
Deployment on VM:
Build latest Docker images: web, API, compute-engine.
Start all containers via docker compose -f docker-compose.production.yml up -d.
Verify services: curl http://localhost/api/health for health check, template page renders without JS errors, login & forgot-password flows work.
Full Module Verification:
Dashboard, Entity Builder, Document Processor, Scorecards, Extraction, Admin modules all functional.
Ensure API and web server communicate correctly.
Deliverables:
Corrected nginx.conf and Docker Compose configuration.
Updated frontend components with safe data handling.
Authentication & forgot-password backend routes.
Email sending configured for personal email addresses.
Instructions for building, deploying, and verifying app on VM.
Constraints:
Maintain current VM and Docker architecture.
Do not break existing API or frontend routes.
Ensure SSL, CORS, and all module integrations work.