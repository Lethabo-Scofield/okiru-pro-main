Continue from the completed T001–T005 work.

The previous implementation fixed several form/model issues, but the original feedback is not fully closed yet. Please now finish the remaining verification and fixes from the B-BBEE Toolkit feedback.

Focus on these remaining items:

1. Date validation

Add browser-level tests for the financial period date fields.

Expected:

* `01/01/2026` is accepted if this is the required app format.
* Valid dates do not show suggestions or warnings.
* Invalid dates show a clear validation error.
* The app should not suggest a different format when the date is already valid.
* Date format must be consistent across the toolkit.

If the app currently uses `YYYY-MM-DD`, confirm whether the product requirement is `DD/MM/YYYY` or ISO format, then make the UI consistent.

2. Numeric input crash

Add browser-level tests for all number inputs that previously crashed.

Test:

* Ownership: years held, outstanding debt, black ownership %, black women ownership %, disabled ownership %, youth ownership %, voting rights %.
* Management Control: annual salary, voting rights %.
* Skills Development: travel costs, programme spend, payroll-related spend, bursary spend, participation counts.
* Procurement/Supplier Development numeric fields.

Expected:

* User can click once and type.
* No page crash.
* No forced refresh.
* No console error containing:
  `input element's type "number" does not support selection`
* Values save and persist after refresh.

Use Playwright or Cypress if available. If no browser test framework exists, add Playwright.

3. Scorecard calculation verification

The original feedback says data can be entered and saved, but the scorecard does not read or calculate it correctly.

Add tests proving this full flow:

Input data → Save → Read saved data → Calculate scorecard → Display score → Refresh → Score still correct

Cover:

* Skills Development payroll spend.
* Skills Development bursary expenditure.
* Black people participating in learnerships/apprenticeships/internships.
* Preferential Procurement supplier data.
* Supplier Development data.

Expected:

* Valid qualifying input must not produce zero scores.
* Scorecard sections must update after save/recalculate.
* Refresh should not lose calculated results.
* Scorecard must read persisted data, not only local state.

4. CalculatorConfig baseline

Some existing calculator tests fail because `CalculatorConfig is required`.

Do not ignore this if it affects scorecard calculation.

Please either:

* update the tests to pass a valid default `CalculatorConfig`, or
* create a test helper/factory for valid calculator config, or
* explain clearly why those tests are unrelated and add separate passing tests for the actual scorecard calculation path.

The final result must prove that scorecard calculation works with real valid config.

5. Backend persistence for new fields

Confirm whether these new fields persist to backend/storage:

* employee.annualSalary
* employee.votingRightsPercent
* supplier.registrationNumber
* skills municipality
* custom municipality value if “Other” is selected

Expected:

* Save to backend.
* Reload client.
* Values still appear.
* Scorecard can use the saved data where relevant.

6. Data Date

Search the whole app for `Data Date`, `dataDate`, `data_date`, and related labels.

Expected:

* If the field truly does not exist, add a regression test or documented assertion confirming it is absent from Ownership and Skills Development.
* If it appears anywhere in the UI, rename or remove it depending on its purpose.

7. Final report

After completing the work, provide a concise report:

* Files changed.
* Tests added.
* Tests passing.
* Any remaining failing tests.
* Whether browser-level tests were added.
* Whether backend persistence is confirmed.
* Whether scorecard calculation is proven end-to-end.
* Any assumptions made.
