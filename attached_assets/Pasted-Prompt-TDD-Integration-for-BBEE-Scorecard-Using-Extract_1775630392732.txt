Prompt: TDD Integration for BBEE Scorecard Using Extracted Entities
Objective

Implement integration of extracted entities into the existing BBEE scoring pipeline using Test-Driven Development (TDD). Ensure behavior matches the manual input system exactly.

TDD Strategy

Follow strict Red → Green → Refactor cycle.

1. Define Test Cases First (RED)
Test Suite: Entity → Scorecard Pipeline

Test 1: Valid Extracted Entities

Input: Complete, valid extracted entities
Expected:
Correct mapping to toolkit structure
clientInfoToToolkitClient produces valid output
calculateFromPillarData is called
Scorecard is generated
Output matches manual input result

Test 2: Missing Required Fields

Input: Incomplete extracted entities
Expected:
Validation fails
calculateFromPillarData is NOT called
No scorecard generated
No fallback to mock data

Test 3: Exact Parity with Manual Flow

Input: Same data as AutoFillButton.tsx
Expected:
Extracted pipeline output === manual pipeline output

Test 4: No Mock Usage

Input: Any extracted data
Expected:
System never loads “Thandani Transport”
Mock path is not triggered

Test 5: Store Hydration

Input: Valid extracted entities
Expected:
Store state matches manual hydration structure
Pillar data correctly populated
2. Minimal Implementation (GREEN)

Implement only enough logic to pass tests:

Create mapping function:

mapExtractedEntitiesToToolkitInput(entities)

Add validation layer:

validateMinimumFields(mappedData)

Integration flow:

entities
  → map
  → validate
  → clientInfoToToolkitClient
  → calculateFromPillarData
3. Refactor
Remove duplication between manual and extracted flows
Ensure single source of truth for:
mapping rules
validation rules
4. Test Structure (Example)
describe("Extracted Entities → Scorecard", () => {
  it("generates scorecard for valid entities", () => {
    // arrange
    const entities = mockValidEntities();

    // act
    const result = runExtractionPipeline(entities);

    // assert
    expect(result.scorecard).toBeDefined();
  });

  it("fails when required fields are missing", () => {
    const entities = mockIncompleteEntities();

    const result = runExtractionPipeline(entities);

    expect(result.scorecard).toBeUndefined();
  });

  it("matches manual flow output", () => {
    const entities = fromAutoFillData();

    const extractedResult = runExtractionPipeline(entities);
    const manualResult = runManualPipeline();

    expect(extractedResult).toEqual(manualResult);
  });
});
5. Constraints
Do not modify scoring logic
Do not introduce mock fallbacks
Tests must drive all implementation
Ensure deterministic outputs
6. Final Output

A fully tested pipeline:

Extracted Entities
  → Mapping
  → Validation
  → Transformation
  → Store Hydration
  → Scorecard

With:

100% coverage on integration path
Guaranteed parity with manual system