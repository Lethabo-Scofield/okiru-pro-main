You are working on the B-BBEE Toolkit app.

Please write and run tests to confirm that all feedback from the uploaded B-BBEE Toolkit Feedback PDF has been fixed. Focus on regression tests, UI behavior tests, validation tests, save/load tests, and scorecard calculation tests.

Test the following areas:

1. Date validation

Create tests for the financial period date fields.

Expected behavior:

* Correct dates such as `01/01/2026` should be accepted.
* No warning or suggestion should appear when the date is valid.
* The system should not suggest a different format when the current input is already correct.
* Invalid dates should show a clear validation message.
* Date format should be consistent across Company Information and all related sections.

2. “Data Date” field validation

Search the app for all `Data Date` fields.

Expected behavior:

* If `Data Date` was removed, tests should confirm it no longer appears in Ownership or Skills Development.
* If it was renamed, tests should confirm the new name is clear and consistent.
* The field should not appear in places where it does not belong.
* Required validation should only apply if the field is actually needed.

3. Ownership table numeric inputs

Create tests for the Ownership section.

Test these columns:

* Years Held
* Outstanding Debt
* Black Ownership %
* Black Women Ownership %
* Disabled Ownership %
* Youth Ownership %
* Voting Rights %

Expected behavior:

* User can click once and type into numeric cells.
* Numeric input should not crash the app.
* The app must not throw: `input element's type "number" does not support selection`.
* Values should save correctly.
* Values should persist after page refresh.
* Validation should allow valid percentages and reject invalid values like negative numbers or values above 100 where applicable.

4. Ownership Type field

Create tests for the Ownership Type field.

Expected behavior:

* If Ownership Type remains, it must provide clear selectable options.
* User should not be forced to guess what the field means.
* Empty/invalid ownership type should show a useful validation message if required.

5. Management Control numeric inputs

Create tests for the Management Control section.

Test these columns:

* Annual Salary
* Voting Rights %
* Disabled status if applicable
* Any other numeric management control fields

Expected behavior:

* User can enter annual salary.
* User can enter voting rights percentage.
* User should not need to click three times before typing.
* Numeric input should not crash the app.
* The app must not throw the number input selection error.
* The Total Annual Salary should appear in the correct place.
* Total Annual Salary should update correctly after salary values are entered.
* Saved values should persist after page refresh.

6. Skills Development municipality field

Create tests for municipality input under Skills Development.

Expected behavior:

* Municipality field should provide selectable municipality options.
* User should be able to choose an existing municipality.
* User should be able to select `Other` or enter a custom municipality if the municipality is not listed.
* Saved municipality values should persist after refresh.

7. Skills Development numeric inputs

Create tests for Skills Development input fields.

Test:

* Travel costs
* Programme spend
* Payroll-related spend
* Bursary spend
* Learnership/apprenticeship/internship participation numbers
* Disabled black employee learning programme spend, if applicable

Expected behavior:

* User can enter numeric values without the app crashing.
* The number input selection error must not occur.
* Values should save correctly.
* Values should persist after refresh.
* Entered values should be included in the scorecard calculation.

8. Preferential Procurement terminology

Create tests for terminology in Preferential Procurement.

Expected behavior:

* The enterprise size option should use `Generic`, not `Large`.
* `Large` should not appear where the correct B-BBEE terminology is `Generic`.
* Existing saved data should map correctly if old records used `Large`.

9. Supplier Registration Number column

Create tests for the Preferential Procurement supplier table.

Expected behavior:

* Supplier table includes an optional `Registration Number` column.
* User can save a supplier with a Registration Number and no VAT Number.
* User can save a supplier with a VAT Number and no Registration Number.
* User can save both fields if provided.
* Supplier data should persist after refresh.
* Scorecard calculation should not fail when VAT Number is missing but Registration Number exists.

10. Skills Development scorecard calculation

Create tests to confirm Skills Development scores calculate correctly.

Expected behavior:

* Payroll spend should feed into Skills Development score.
* Bursary spend should feed into Skills Development score.
* Learning programme spend for black people should feed into the score.
* Number of black people participating in learnerships, apprenticeships, and internships should feed into the score.
* Scores should not remain zero when valid qualifying data exists.
* Scorecard should update after save/recalculate.
* Refreshing the page should not reset calculated scores.

11. Preferential Procurement scorecard calculation

Create tests to confirm Preferential Procurement data flows into the scorecard.

Expected behavior:

* Supplier entries should save correctly.
* Supplier data should be read by the scorecard engine.
* Preferential Procurement score should calculate when valid procurement data exists.
* Score should not remain zero if qualifying supplier data was entered.
* Scorecard should update after save/recalculate.
* Data should persist after refresh.

12. Supplier Development scorecard calculation

Create tests to confirm Supplier Development data flows into the scorecard.

Expected behavior:

* Supplier Development entries should save correctly.
* Saved data should be read by the scorecard engine.
* Supplier Development score should calculate when valid qualifying data exists.
* Score should not remain zero if valid data was entered.
* Scorecard should update after save/recalculate.
* Data should persist after refresh.

13. End-to-end scorecard flow

Create an end-to-end test covering this full flow:

Company Information → Ownership → Management Control → Skills Development → Preferential Procurement → Supplier Development → Save → Calculate Scorecard → Refresh → Reopen Scorecard

Expected behavior:

* No crashes occur.
* No invalid validation warnings appear for correct data.
* All saved inputs remain after refresh.
* All relevant scorecard categories read saved data correctly.
* Grand total updates correctly.
* Categories with valid input should not incorrectly show zero.
* The final scorecard should reflect the entered data.

14. Console and runtime error checks

Add tests that fail if the browser console contains serious runtime errors.

Specifically fail the test if this error appears:

`input element's type "number" does not support selection`

Also fail tests on uncaught exceptions, application error screens, or reload-required crashes.

Implementation instructions:

* Use the existing test framework in this Replit project.
* If the app uses Playwright or Cypress, create browser-based tests for the table input behavior.
* If the app already has unit tests for score calculations, add focused unit tests for the calculation engine.
* Add regression tests so these bugs cannot return.
* Run the full test suite after creating the tests.
* Provide a short report showing:

  * Tests added
  * Files changed
  * Bugs covered
  * Any tests still failing
  * Any assumptions made
